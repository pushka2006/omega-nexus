{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "CI=false npm run build",
  "rewrites": [
    {
      "source": "/deployed/:path*",
      "destination": "/apps/:path*"
    },
    {
      "source": "/apps/:path*",
      "destination": "/apps/:path*"
    },
    {
      "source": "/((?!apps/|deployed/|static/|assets/|favicon\\.ico|manifest\\.json|robots\\.txt).*)",
      "destination": "/index.html"
    }
  ]
}
