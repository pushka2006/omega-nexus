{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "CI=false npm run build",
  "env": {
    "CI": "false"
  },
  "build": {
    "env": {
      "CI": "false"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
