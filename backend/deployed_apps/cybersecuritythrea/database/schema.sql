-- Generated by Database Engineer AI — OMEGA NEXUS OS
-- Project: Cybersecuritythrea | App Type: GENERIC
-- Target: PostgreSQL 16 / SQLite 3

CREATE TABLE IF NOT EXISTS cybersecuritythrea_items (
    id         VARCHAR(64) PRIMARY KEY,
    title      VARCHAR(500) NOT NULL,
    category    VARCHAR(255) DEFAULT 'General',
    score    VARCHAR(255) DEFAULT '',
    status     VARCHAR(50) DEFAULT 'ACTIVE',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_cybersecuritythrea_items_status ON cybersecuritythrea_items(status);
CREATE INDEX IF NOT EXISTS idx_cybersecuritythrea_items_title  ON cybersecuritythrea_items(title);

-- Seed Data
INSERT INTO cybersecuritythrea_items (id, title, status) VALUES
    ('1', 'Cybersecuritythrea Core Engine', 'ACTIVE'),
    ('2', 'AI Telemetry Pipeline', 'ACTIVE'),
    ('3', 'Security Cluster', 'VERIFIED')
ON CONFLICT (id) DO NOTHING;
